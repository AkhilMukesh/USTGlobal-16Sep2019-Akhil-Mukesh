//Random number between 1 to 100.
console.log(Math.floor(Math.random()*100));

//function to find factorial of a number
var num = 3;
function fact(num)
{
   var x=parseInt(num);
   //alert(x+1);
   if(x>0)
       x=x* fact(x-1);
   console.log(x);
   
}
    
//function to find fibonacci series

//function to find circumferences of a number
//function to add array number in an array and return total
//function to find whether the number is prime or not.

